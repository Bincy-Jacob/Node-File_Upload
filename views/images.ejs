<!DOCTYPE html>
<html>
  <head>
    <title>Images</title>
  </head>
  <body>
    <h1>Images</h1>
    <ul>
      <% images.forEach(image => { %>
      <li>
        <img src="/images/<%= image %>" alt="<%= image %>" />
      </li>
      <% }) %>
    </ul>
  </body>
</html>
